import { initialize, move } from '../endpointCalls'

function depthFirstTraversal() {
    let stack = []

    // get current room { coordinates, roomId, exits }
    // add to {roomId: [coordinates, exits]} in 
    // add room w/ exits to stack
    // move in a direction
        //
}